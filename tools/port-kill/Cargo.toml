[package]
name = "port-kill"
version = "0.1.0"
edition = "2024"
description = "Interactively stop a listening TCP service by port"
license = "MIT"
publish = false

[dependencies]
crossterm = "0.29.0"
libc = "0.2"
ratatui = { version = "0.30.2", features = ["crossterm_0_29"] }
serde_json = "1"

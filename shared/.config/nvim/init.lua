-- LazyVim's extras/news metadata is machine-local. Plugin revisions remain
-- shared and reproducible through lazy-lock.json.
vim.g.lazyvim_json = vim.fn.stdpath("state") .. "/lazyvim.json"

require("config.lazy")
